import 'package:flutter/material.dart';

class ecran2 extends StatelessWidget{
  final String message;

  const ecran2(this.message);

  @override
  Widget build(BuildContext context) {

    throw UnimplementedError();
  }
}